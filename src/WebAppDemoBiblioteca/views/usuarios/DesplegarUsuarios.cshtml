@model  usuarios.usuarios

<div class="col-lg-3 col-md-6">
    <div class="card">
        <img class="card-img-top" src="@Model.ImagenUsuarioURL" alt="">
        <div class="card-body">
               <h4 class="card-tittle">@Model.NombreUsuario</h4>
            <p class="card-text">@Model.UsernameUsuario</p>
        </div>
        <div class="card-footer">
            <a href="#" class="btn btn-primary">Detalles</a>
        </div>

    </div>

</div>